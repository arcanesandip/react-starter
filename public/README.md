# Public

Place static files here that need to be publicly accessible at the root URL.
Vite serves these as-is — they are NOT processed or optimized by the build.

Examples:
- favicon.ico
- robots.txt
- og-image.png
